#include "main.h"

/**
 * _isupper - checks if parameter is an uppercase letter.
 * @c: input character.
 * Return: 1 if is an uppercase character, 0 in other case.
 */
{
	if (c >= 65 && c <= 90)
	{
		Return: (1);
	}
	else
	{
		Return: (0);
	}
}
